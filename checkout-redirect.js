function openRequestedCheckout(){if(new URLSearchParams(location.search).get('checkout')!=='1')return;let tries=0;const timer=setInterval(()=>{tries++;const cart=JSON.parse(localStorage.getItem('mirella-cart')||'[]');const cartBtn=document.getElementById('cartBtn');if(cart.length&&cartBtn){cartBtn.click();setTimeout(()=>document.getElementById('checkoutBtn')?.click(),250);clearInterval(timer)}if(tries>80)clearInterval(timer)},250)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',openRequestedCheckout);else openRequestedCheckout();