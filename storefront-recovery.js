import{getApps,getApp,initializeApp}from'https://www.gstatic.com/firebasejs/12.1.0/firebase-app.js';import{getFirestore,doc,getDoc,collection,getDocs}from'https://www.gstatic.com/firebasejs/12.1.0/firebase-firestore.js';import{firebaseConfig}from'./firebase-config.js';
const app=getApps().length?getApp():initializeApp(firebaseConfig),db=getFirestore(app),$=id=>document.getElementById(id),esc=s=>String(s??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
function safeCart(){try{const v=JSON.parse(localStorage.getItem('mirella-cart')||'[]');return Array.isArray(v)?v:[]}catch{localStorage.removeItem('mirella-cart');return[]}}
function money(n){return'₹'+Number(n||0).toLocaleString('en-IN')}
function applyMedia(store){const m=store.homepageMedia||{};const bg=(sel,url)=>{const el=document.querySelector(sel);if(el&&url){el.style.backgroundImage=`url("${String(url).replace(/"/g,'\\"')}")`;el.style.backgroundSize='cover';el.style.backgroundPosition='center';el.classList.add('has-store-image')}};bg('.hero-visual',m.hero?.imageUrl);bg('.editorial-image',m.editorial?.imageUrl);bg('.story-art',m.story?.imageUrl);document.querySelectorAll('.category').forEach((el,i)=>{const url=m[`category${i+1}`]?.imageUrl;if(url){el.style.backgroundImage=`url("${String(url).replace(/"/g,'\\"')}")`;el.style.backgroundSize='cover';el.style.backgroundPosition='center'}});let style=document.getElementById('storefrontRecoveryStyle');if(!style){style=document.createElement('style');style.id='storefrontRecoveryStyle';style.textContent='.hero-visual.has-store-image:before{display:none!important}.hero-visual.has-store-image .hero-visual-art{background:inherit!important;background-image:inherit!important;background-size:cover!important;background-position:center!important;transform:none!important}.story-art.has-store-image span{display:none!important}';document.head.appendChild(style)}const track=$('announcementTrack');if(track){const a=Array.isArray(store.announcementSlides)?store.announcementSlides.filter(Boolean):[];const data=a.length?a:['Free shipping on orders above ₹999','New beauty drops, curated for your glow','Easy UPI checkout + payment proof'];track.innerHTML=data.map(x=>`<span>${esc(typeof x==='string'?x:x.text||'')}</span>`).join('')}}
function card(p){const stock=Number(p.stock||0);return`<article class="product-card"><div class="product-media"><a href="product.html?id=${encodeURIComponent(p.id)}"><img src="${esc(p.imageUrl||'')}" alt="${esc(p.name)}" loading="lazy"></a><span class="product-badge">${stock>0?esc(p.badge||(p.newArrival?'New':p.bestseller?'Bestseller':'Curated')):'Sold out'}</span></div><div class="product-info"><div class="product-kicker">${esc(p.category||'Beauty')}</div><h3>${esc(p.name)}</h3><p>${esc(p.description||'A beautiful addition to your daily ritual.')}</p><div class="product-bottom"><strong>${money(p.price)}</strong><span>${stock>0?'In stock':'Unavailable'}</span></div></div></article>`}
async function recover(){const cart=safeCart();try{const s=await getDoc(doc(db,'settings','store'));const store=s.exists()?s.data():{};applyMedia(store);const posters=Array.isArray(store.banners)?store.banners.filter(x=>x?.imageUrl):[];const area=$('posterShowcase');if(area&&posters.length&&!area.querySelector('img'))area.innerHTML=`<div class="poster-track">${posters.map(p=>`<article class="poster-card"><img src="${esc(p.imageUrl)}" alt="${esc(p.title||'Mirella Glow promotion')}" loading="lazy"><div class="poster-copy"><span>${esc(p.eyebrow||'Mirella Glow')}</span><h3>${esc(p.title||'Beauty, refined.')}</h3>${p.subtitle?`<p>${esc(p.subtitle)}</p>`:''}</div></article>`).join('')}</div>`;const snap=await getDocs(collection(db,'products'));const products=snap.docs.map(d=>({id:d.id,...d.data()})).filter(p=>p.active!==false);const productsEl=$('products');if(productsEl&&!productsEl.querySelector('.product-card')&&products.length)productsEl.innerHTML=products.map(card).join('');['newProducts','bestProducts'].forEach(id=>{const el=$(id);if(!el||el.querySelector('.product-card'))return;const list=id==='newProducts'?products.filter(p=>p.newArrival||p.new):products.filter(p=>p.bestseller||p.bestSeller);if(list.length)el.innerHTML=list.map(card).join('')});const count=$('cartCount');if(count)count.textContent=cart.reduce((n,x)=>n+Number(x.qty||0),0)}catch(e){console.error('[Mirella Glow] storefront backend recovery failed:',e);const el=$('products');if(el&&!el.children.length)el.innerHTML='<div class="empty">We are refreshing the storefront. Please try again in a moment.</div>'}}
setTimeout(recover,500);